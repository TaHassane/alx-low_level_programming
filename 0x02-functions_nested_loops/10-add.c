#include <stdio.h>
#include <unistd.h>
#include "main.h"

/**
 * add -  adds two integers and returns the result
 * @a: input 1
 * @b: input 2
 * Return: sum of a and b
*/

int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
